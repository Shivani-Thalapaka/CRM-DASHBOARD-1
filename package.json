{
  "name": "crm-dashboard",
  "version": "1.0.0",
  "description": "Modern CRM Dashboard for managing customers, leads, contacts, and sales pipeline",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "install-backend": "cd backend && npm install",
    "setup": "npm run install-backend && cd backend && node initDatabase.js && node seedData.js",
    "build": "echo 'No build step required for this application'"
  },
  "keywords": ["crm", "dashboard", "nodejs", "express", "postgresql"],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/crm-dashboard.git"
  }
}